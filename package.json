{
  "name": "clipboard-saver",
  "version": "1.0.1",
  "description": "Auto-save system clipboard contents (with timestamp) to the current note.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "clipboard"
  ],
  "author": "WorkBuddy",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.21.5",
    "obsidian": "^1.7.2",
    "tslib": "^2.6.3",
    "typescript": "^5.5.3"
  }
}
